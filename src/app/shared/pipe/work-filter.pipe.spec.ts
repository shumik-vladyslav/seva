import { WorkFilterPipe } from './work-filter.pipe';

describe('WorkFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new WorkFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
